<%= form_for(@stared) do |f| %>
  <% if @stared.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@stared.errors.count, "error") %> prohibited this stared from being saved:</h2>

      <ul>
      <% @stared.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :event_id %><br />
    <%= f.number_field :event_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
